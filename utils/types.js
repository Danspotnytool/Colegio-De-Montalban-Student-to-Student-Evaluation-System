
// This is a JSDoc types file. It's used to document JavaScript code.
// It's not required, but it's a good practice to use it.

/**
 * @typedef {{
 *      code: Number,
 *      payload: {} | {}[],
 *      status: String,
 * }} Response
 */

/**
 * @typedef {{
 *      firstName: String
 *      middleName: String
 *      lastName: String
 *      studentNumber: String
 *      gender: "male" | "female"
 *      birthday: String
 *      profilePicture: String
 *      email: String
 *      course: String
 *      section: String
 *      year: String
 *      password: String
 *      token: String
 *      timeAdded: String
 * }} Student
 */


// This part is unrelated to the above snippet, but it's required as a workaround to export types.
module.exports = {};